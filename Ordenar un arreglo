
void ordenar (int arreglo[])
{
   int i, j, temporal;
 
   for (i = tam - 1; i > 0; i--)
      for (j = 0; j < i; j++)
         if (arreglo[j] > arreglo[j + 1]) {
            temporal = arreglo[j];
            arreglo[j] = arreglo[j + 1];
            arreglo[j + 1] = temporal;
         }
}
